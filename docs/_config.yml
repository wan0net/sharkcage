title: sharkcage
description: OpenClaw inside a kernel-level sandbox. Per-skill ASRT sandboxing, audited approvals, and a tamper-evident local audit trail.
url: https://wan0.net
baseurl: /sharkcage
markdown: kramdown
kramdown:
  input: GFM
  syntax_highlighter: rouge
exclude:
  - _config.yml
