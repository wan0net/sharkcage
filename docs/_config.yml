title: sharkcage
description: OpenClaw inside a kernel-level sandbox. Per-skill ASRT sandboxing, capability model, approve once enforce always.
url: https://wan0.net
baseurl: /sharkcage
markdown: kramdown
kramdown:
  input: GFM
  syntax_highlighter: rouge
exclude:
  - _config.yml
